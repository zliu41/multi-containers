steps:
  - label: GHC 8.6.5
    command: "runhaskell TestGen && stack --resolver lts-13.21 test"
    branches: "master"

  - label: GHC 8.4.4
    command: "runhaskell TestGen && stack --resolver lts-12.26 test"
    branches: "master"

  - label: GHC 8.2.2
    command: "runhaskell TestGen && stack --resolver lts-11.22 test"
    branches: "master"
